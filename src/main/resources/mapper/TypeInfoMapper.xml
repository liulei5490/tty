<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.orc//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.blog.mapper.ITypeInfoMapper">
   
   <select id="findAll" resultType="TypeInfo" >
		select type,typename from typeinfo order by type;
   </select>
   
   <select id="finds" resultType="TypeInfo" >
   		select type,typename from typeinfo order by type;
   </select>
   
  
</mapper>